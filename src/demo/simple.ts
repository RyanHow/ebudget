

/*
* Create a new budget
*/
import { DemoRunner } from "./demo-runner";

export class SimpleDemo {

    run(demo: DemoRunner) {

        // Set up the starting environment (Not sure how to do this ?!?!?, database and everything ??, maybe just the page so this can be overriden ?)

                

        // MoveToClick Create New Budget button

        // FocusAndType Budget Name

        // MoveToClick Ok button
    }

}